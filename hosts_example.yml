all:
    children:
        ubuntu:
            hosts:
                $HOSTS:
        debian:
            hosts:
                $HOSTS:
        installenv:
            hosts:
                $HOSTS:
        installtelegraf:
            hosts:
                $HOSTS:
        installboinc:
            hosts:
                $HOSTS:
        installdocker:
            hosts:
                $HOSTS:
